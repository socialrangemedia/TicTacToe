# Node.js Docker (Tic Tac Toe)
#
# Gebruik: copy_template met template=nodejs-docker en targetDir=docker
# Start:   docker compose -f docker/docker-compose.yml up -d
# Logs:    docker compose -f docker/docker-compose.yml logs -f app
# Stop:    docker compose -f docker/docker-compose.yml down
#
# App-code staat in de workspace-root (één map boven docker/).
# Zorg dat package.json een "dev" script heeft dat op poort 3000 luistert.
